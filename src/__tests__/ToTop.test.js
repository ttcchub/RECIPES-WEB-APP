// testing ToTop function

describe('ScrollToTop function', () => {
  test('It scrolls window to top, when navigating to a new page', () => {

    jest.spyOn(window, 'scroll').mockImplementation(() => { });

});
});
